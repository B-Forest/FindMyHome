{{ form_start(visitForm) }}
    {{ form_row(visitForm.dateStart , {'attr': {'class': 'flex justify-center items-center mb-4 shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg block w-full p-2.5'}}) }}
    {{ form_row(visitForm.dateEnd , {'attr': {'class': 'flex justify-center items-center mb-4 shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg block w-full p-2.5'}}) }}
    {{ form_row(visitForm.property , {'attr': {'class': 'flex justify-center items-center mb-4 shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg block w-full p-2.5'}}) }}

<div class="flex justify-center items-center">
    <button class="btn text-white bg-gradient-to-r from-teal-400 via-teal-500 to-teal-600 hover:bg-gradient-to-br font-medium rounded-lg text-sm px-16 py-6 text-center mr-2 mb-2">{{ button_label|default('Créer une visite') }}</button>
</div>
{{ form_end(visitForm) }}
