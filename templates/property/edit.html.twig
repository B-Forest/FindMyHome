{% extends 'base.html.twig' %}

{% block title %}Edit Property{% endblock %}

{% block body %}
    <section>
        <div class="flex flex-col justify-center items-center">
            <h2>Modifier les informations de votre propriété</h2>

            {{ include('property/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('profile', {'id': app.user.id} ) }}"
               class="text-white bg-main font-medium rounded-lg text-sm px-5 py-2.5 text-center mr-2 mb-2">Retour a
                votre profil</a>
        </div>
    </section>
{% endblock %}
