<form method="post" action="{{ path('app_visit_admin_delete', {'id': visit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visit.id) }}">
    <button class="btn">Supprimer</button>
</form>
