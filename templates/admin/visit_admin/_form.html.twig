<div class="flex justify-center items-center">
{{ form_start(form) }}
    {{ form_row(form.dateStart , {'attr': {'class': 'w-[28rem] flex justify-center items-center mb-4 shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg block p-2.5'}}) }}
    {{ form_row(form.dateEnd , {'attr': {'class': 'w-[28rem] flex justify-center items-center mb-4 shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg block p-2.5'}}) }}
    {{ form_row(form.visitor , {'attr': {'class': 'w-[28rem] flex justify-center items-center mb-4 shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg block p-2.5'}}) }}

    <div class="flex justify-center items-center">
        <button type="submit" class="bg-valid font-medium rounded-lg text-sm px-5 py-2.5 text-center mr-2 mb-2">{{ button_label|default('Créer') }}
        </button>
    </div>
{{ form_end(form) }}
</div>
