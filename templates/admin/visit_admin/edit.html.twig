{% extends 'admin/base.html.twig' %}

{% block title %}Edit Visit{% endblock %}

{% block body %}
    <section>
        <h2>Edit Visit</h2>
        <div class="flex flex-col justify-center items-center">
        {{ include('admin/visit_admin/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <a href="{{ path('app_visit_admin_index') }}" class="text-white bg-main font-medium rounded-lg text-sm px-5 py-2.5 text-center mr-2 mb-2">Retour</a>
    </section>
{% endblock %}
